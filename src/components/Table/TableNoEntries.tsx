import { Typography } from "@mui/material";

const TableNoEntries = () => (
  <Typography
    marginLeft="10px"
    marginTop="4px"
    fontSize={16}
    fontFamily="monospace"
  >
    No entries found.
  </Typography>
);

export default TableNoEntries;
